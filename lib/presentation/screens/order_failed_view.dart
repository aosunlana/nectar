import 'package:flutter/material.dart';

class OrderFailedView extends StatelessWidget {
  const OrderFailedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
