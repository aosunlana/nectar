import 'package:flutter/material.dart';

class LogonView extends StatelessWidget {
  const LogonView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
